# CodeSaver CLI v1.3.2

## Highlights

- Added `--top-directories` and `--empty-directories` for project layout analysis.
- Added `--archive-age` and `--archive-size` for archive lifecycle checks.
- Added `--git-branch` and `--git-commit` for lightweight CI metadata.
- Added `--excluded-paths` to audit active filtering rules.
- Added `--backup-dir-check` to validate backup destination access.

## Validation

- 46 tests passed locally.
- Black and Flake8 passed.
