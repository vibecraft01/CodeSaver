# CodeSaver CLI v1.3.6

## Highlights

- Added `--version` for support and automation scripts.
- Added `--plan-json FILE` for auditable backup planning.
- Added `--verify-report FILE` for persistent archive-health reports.
- Added `--git-remote` for repository diagnostics.
- Added `--config-template FILE` for quick configuration setup.

## Validation

- Full test suite passed locally.
- Black and Flake8 passed.
- CLI smoke tests passed locally.
