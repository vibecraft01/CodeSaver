# CodeSaver CLI v1.3.8

## Highlights

- Added `--archive-types` for archive member type summaries.
- Added `--hash-inventory` for project SHA-256 inventory export.
- Added `--backup-timeline` for chronological backup history.
- Added `--empty-files` for project audits.
- Added `--git-remotes-json` for structured Git remote output.

## Validation

- Full test suite passed locally.
- Black formatting passed locally.
- CLI smoke tests passed locally.
