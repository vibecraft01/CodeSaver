# CodeSaver CLI v1.3.5 (local)

## Highlights

- Added `--gitignored-files` for auditing Git exclusions.
- Added `--project-tree` with size-aware file listings.
- Added `--search-content TEXT` for source-code searches.
- Added `--extension-report` with counts and storage totals.
- Added `--health-report` with per-archive verification results.

## Validation

- Full test suite passed locally.
- Black and Flake8 passed.
- CLI smoke tests passed locally.
