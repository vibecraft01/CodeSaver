# CodeSaver CLI v1.4.2

## Highlights

- Added `--project-tree-json` for project inventory export.
- Added `--archive-member-count` for archive inspection.
- Added `--git-untracked-files` for working-tree audits.
- Added `--backup-oldest` for retention review.
- Added `--project-empty-dirs` for project cleanup audits.

## Validation

- Full test suite passed locally.
- Black formatting passed locally.
- CLI smoke tests passed locally.
