# CodeSaver CLI v1.2.8

## Highlights

- Added `--archive-info` for scriptable archive metadata inspection.
- Added `--export-manifest` to export archive contents for audits and indexing.
- Added `--delete-backup` and `--rename-backup` for backup rotation workflows.
- Added `--find` to locate project files by name, with `--json` output for tooling.

## Validation

- 46 tests passed locally.
- Black and Flake8 passed.
