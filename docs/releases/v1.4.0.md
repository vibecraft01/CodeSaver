# CodeSaver CLI v1.4.0

## Highlights

- Added `--project-checksum` for deterministic project fingerprints.
- Added `--newer-than` for recent-file audits.
- Added `--git-branches` for local branch inspection.
- Added `--backup-sizes` for storage analysis.
- Added `--archive-paths` for JSON backup-path export.

## Validation

- Full test suite passed locally.
- Black formatting passed locally.
- CLI smoke tests passed locally.
